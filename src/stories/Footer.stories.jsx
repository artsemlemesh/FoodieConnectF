import React from 'react';
import Footer from '../components/Footer';
 //footer is incomplete, so we need to create a story for it
export default {
  title: 'Organisms/Footer',
  component: Footer,
};

export const Default = () => <Footer />;